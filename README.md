# Networking-TCP-UDP-Project
This project implements a multi-client Online Reservation System using Java SE. The system is built on TCP Sockets, allowing multiple clients to connect to a central server simultaneously and perform reservation operations in real time.
